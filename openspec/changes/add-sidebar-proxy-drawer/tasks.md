# Tasks: add-sidebar-proxy-drawer

- [x] Add failing sidebar proxy-drawer tests for menu entry, default draft, and save/toggle behavior.
- [x] Extract/reuse proxy state and render it in a non-modal sidebar drawer.
- [x] Wire the existing app-settings persistence callback through the layout-node boundary.
- [x] Remove the duplicate proxy card from Basic settings.
- [x] Reuse localized proxy labels and add scoped drawer styles.
- [x] Run focused tests and OpenSpec validation; typecheck/governance failures are pre-existing concurrent-worktree blockers, documented in `verification.md`.
- [ ] Perform manual visual verification; do not commit.
